apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: workflow-creation-ts-
spec:
  entrypoint: main
  templates:
  - name: main
    container:
      image: alpine:3.23
      command: [sh, -c]
      args: ["echo ts={{workflow.creationTimestamp}}"]
