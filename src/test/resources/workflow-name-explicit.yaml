apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: workflow-name-test-
  name: workflow-name-test-4tjnf
spec:
  entrypoint: main
  templates:
  - name: main
    container:
      image: alpine:3.23
      command: [sh, -c]
      args: ["echo name={{workflow.name}}"]
